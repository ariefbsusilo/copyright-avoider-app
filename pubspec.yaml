name: copyright_avoider_app
description: Aplikasi pembelajaran untuk memahami copyright avoider di YouTube
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  # REMOVED heavy packages for faster web loading:
  # file_picker: ^6.1.1
  # video_player: ^2.8.1
  # flutter_ffmpeg: ^0.4.2
  # path_provider: ^2.1.1
  # permission_handler: ^11.0.1
  # share_plus: ^7.2.1
  # google_fonts: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
